import {
    numberRegexp,
    wordsFromUpperCase,
    emailValid
} from './regexes'

import { normalize } from './normalize'

export default {
    numberRegexp,
    wordsFromUpperCase,
    emailValid,
    normalize
}