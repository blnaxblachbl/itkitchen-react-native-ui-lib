import {
    numberRegexp,
    wordsFromUpperCase,
    emailValid
} from './regexes'
import { normalize } from './normalize'
import hexToRgba from './hexToRgba'

export default {
    numberRegexp,
    wordsFromUpperCase,
    emailValid,
    normalize,
    hexToRgba
}